PORT=5000
NODE_ENV=development

# Firebase Configuration
# Note: The backend uses Firebase Admin SDK with serviceAccountKey.json
# You still need FIREBASE_PROJECT_ID for reference, but the main authentication
# comes from the serviceAccountKey.json file in the backend folder

FIREBASE_PROJECT_ID=your-project-id

# Optional: If you prefer environment variables over serviceAccountKey.json
# You can use these instead (get from Firebase Console → Service Accounts)
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-private-key-here\n-----END PRIVATE KEY-----\n"
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
